import styles from "./Main.module.scss";

const Main = () => {
    return (
        <div className={`${styles.main} title`}>
            <h2 className='title'> Main</h2>
        </div>
    )
}
export default Main;